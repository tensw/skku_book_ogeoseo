# 오거서 플랫폼 최종 프로젝트 기획안

## 1. 프로젝트 개요

### 1.1 목표
기존 오거서 독서 토론 플랫폼 UI에 성균관대 오거서(book.skku.edu)의 핵심 기능 3가지를 통합하고, 관리자 페이지를 추가하여 시연 가능한 완성 프로젝트를 구축한다.

### 1.2 추가 기능
| 기능 | 출처 | 설명 |
|------|------|------|
| 공지사항 | book.skku.edu/notice-event/notice/ | 테이블형 공지 리스트 + 상세 보기 |
| 독서리뷰 | book.skku.edu/read-reviews/ | 프로그램 후기 + 오거서 후기 |
| 독서 가이드 | book.skku.edu/reading-guide/100-classics/ | 성균 고전 100선 |

### 1.3 기술 스택 (변경 없음)
- Next.js 16 (App Router로 전환)
- React 19 + TypeScript
- Tailwind CSS 3 + shadcn/ui
- lucide-react 아이콘
- Next.js API Routes (목업 백엔드)

---

## 2. 정보 구조 (IA)

### 2.1 이용자 페이지 네비게이션

```
GNB (반응형 웹 - 상단 바)
├── 공지사항        /notices
├── 독서리뷰(서평)   /reviews
│   ├── 프로그램 후기  /reviews?tab=program
│   └── 오거서 후기   /reviews?tab=ogoseo
├── 독토            /discussions
├── 독서가이드       /guide
│   └── 성균 고전 100선  /guide/classics
├── 톡톡            /talk
└── 내 서재          /library

모바일 (적응형 - 좌측 햄버거 메뉴)
├── 동일한 메뉴 구조
└── 햄버거 아이콘 (☰) 클릭 시 슬라이드 패널
```

### 2.2 관리자 페이지

```
/admin
├── 대시보드         /admin                  (통계 요약)
├── 공지사항 관리     /admin/notices          (CRUD)
├── 독서리뷰 관리     /admin/reviews          (CRUD)
├── 고전 100선 관리   /admin/classics         (CRUD)
└── 프로그램 관리     /admin/programs         (목록/등록)
```

---

## 3. 라우팅 구조 (App Router)

```
app/
├── layout.tsx                    # 루트 레이아웃 (폰트, 메타데이터)
├── (user)/                       # 이용자 레이아웃 그룹
│   ├── layout.tsx                # GNB + 모바일 햄버거 메뉴
│   ├── page.tsx                  # / → 홈 (리다이렉트 또는 홈 콘텐츠)
│   ├── notices/
│   │   ├── page.tsx              # 공지사항 리스트
│   │   └── [id]/
│   │       └── page.tsx          # 공지사항 상세
│   ├── reviews/
│   │   ├── page.tsx              # 독서리뷰 (탭: 프로그램/오거서)
│   │   └── [id]/
│   │       └── page.tsx          # 리뷰 상세
│   ├── discussions/
│   │   └── page.tsx              # 독토
│   ├── guide/
│   │   ├── page.tsx              # 독서가이드 (성균 고전 100선)
│   │   └── [id]/
│   │       └── page.tsx          # 도서 상세
│   ├── talk/
│   │   └── page.tsx              # 톡톡
│   └── library/
│       └── page.tsx              # 내 서재
├── admin/                        # 관리자 레이아웃
│   ├── layout.tsx                # 사이드바 레이아웃
│   ├── page.tsx                  # 대시보드
│   ├── notices/
│   │   ├── page.tsx              # 공지사항 관리
│   │   └── [id]/
│   │       └── page.tsx          # 공지사항 편집
│   ├── reviews/
│   │   └── page.tsx              # 리뷰 관리
│   ├── classics/
│   │   └── page.tsx              # 고전 100선 관리
│   └── programs/
│       └── page.tsx              # 프로그램 관리
└── api/                          # API Routes (목업 백엔드)
    ├── notices/
    │   ├── route.ts              # GET(리스트), POST(생성)
    │   └── [id]/
    │       └── route.ts          # GET(상세), PUT(수정), DELETE(삭제)
    ├── reviews/
    │   ├── route.ts
    │   └── [id]/
    │       └── route.ts
    ├── classics/
    │   ├── route.ts
    │   └── [id]/
    │       └── route.ts
    └── programs/
        └── route.ts
```

---

## 4. 페이지별 상세 기획

### 4.1 공지사항 (/notices)

#### 리스트 페이지
- **레이아웃**: 테이블형 (번호, 제목, 날짜, 조회수, 첨부)
- **기능**: 검색 (제목/내용), 페이지네이션 (10건/페이지)
- **디자인**: 기존 프로젝트 카드 스타일 적용 (rounded-2xl, shadow-sm)
- **모바일**: 카드형으로 변환 (반응형)

#### 상세 페이지
- **구성**: 제목, 날짜, 조회수, 본문, 첨부파일
- **디자인**: 기존 프로젝트 카드/섹션 스타일 적용

#### 데이터 모델
```typescript
interface Notice {
  id: number
  title: string
  content: string
  author: string
  createdAt: string       // ISO date
  viewCount: number
  isPinned: boolean       // 상단 고정
  attachments: {
    name: string
    url: string
    size: string
  }[]
}
```

### 4.2 독서리뷰 (/reviews)

#### 탭 구성
- **프로그램 후기** (`?tab=program`): 독서 프로그램 참여 후기
- **오거서 후기** (`?tab=ogoseo`): 오거서 도서 독후감

#### 프로그램 후기 탭
- **레이아웃**: 카드 그리드 (2열 데스크탑 / 1열 모바일)
- **카드 구성**: 책 썸네일, 프로그램 분류 뱃지, 제목, 저자, 리뷰 미리보기, 작성자, 좋아요/댓글 수
- **필터**: 프로그램별 (오거서 장학금, 독서동아리, 교수-학생 함께 책읽기, 독서챌린저 등)
- **정렬**: 최신순, 인기순, 추천순

#### 오거서 후기 탭
- **레이아웃**: 리스트형
- **항목 구성**: 책 제목, 저자, 리뷰 발췌, 작성일, 조회수
- **정렬**: 작성일순, 제목순
- **기능**: 글쓰기 버튼 → 기존 ReviewWriter 스테퍼 재활용

#### 데이터 모델
```typescript
interface BookReview {
  id: number
  type: 'program' | 'ogoseo'
  programCategory?: string      // 프로그램 분류
  book: {
    title: string
    author: string
    publisher: string
    cover: string
    isbn?: string
  }
  reviewer: {
    name: string
    avatar: string
    department?: string
  }
  title: string
  content: string
  rating: number                // 1-5
  likes: number
  comments: number
  viewCount: number
  createdAt: string
  tags: string[]
}
```

### 4.3 독서가이드 - 성균 고전 100선 (/guide)

#### 리스트 페이지
- **레이아웃**: 카드 그리드 (3열 데스크탑 / 2열 태블릿 / 1열 모바일)
- **카드 구성**: 책 표지, 제목, 저자, 출판사, 카테고리 뱃지
- **필터**: 연도 선택 (2015-2026), 카테고리 (전체/문학·예술/인문·사회/자연과학)
- **검색**: 도서명/저자 검색
- **추가 기능**: 엑셀 다운로드 버튼 (시연용)

#### 상세 페이지
- **구성**: 책 표지, 제목, 저자, 출판사, 카테고리, 소개문
- **연관 정보**: 해당 도서의 리뷰 리스트 연동

#### 데이터 모델
```typescript
interface Classic100 {
  id: number
  year: number                  // 2015-2026
  category: '문학/예술' | '인문/사회' | '자연과학'
  title: string
  author: string
  publisher: string
  cover: string
  description: string
  isbn?: string
}
```

### 4.4 기존 페이지 (유지)

| 페이지 | 경로 | 변경사항 |
|--------|------|----------|
| 독토 | /discussions | 기존 코드 이관, 라우팅만 변경 |
| 톡톡 | /talk | 기존 코드 이관, 라우팅만 변경 |
| 내 서재 | /library | 기존 코드 이관, 라우팅만 변경 |

### 4.5 관리자 페이지 (/admin)

#### 대시보드
- 전체 통계 카드: 공지 수, 리뷰 수, 고전 100선 도서 수, 회원 수
- 최근 활동 리스트

#### 공지사항 관리
- 리스트: 테이블 (체크박스 선택, 일괄 삭제)
- 등록/수정: 폼 (제목, 내용, 상단 고정 여부, 첨부파일)

#### 독서리뷰 관리
- 리스트: 테이블 (유형 필터, 승인/반려)
- 상세: 리뷰 내용 확인 + 상태 변경

#### 고전 100선 관리
- 리스트: 연도별 필터 + 테이블
- 등록/수정: 폼 (도서 정보, 카테고리, 표지 이미지 URL)

#### 프로그램 관리
- 리스트: 프로그램 목록
- 등록: 폼 (프로그램명, 설명, 기간)

---

## 5. UI/UX 설계

### 5.1 반응형 웹 레이아웃 (데스크탑)

```
┌──────────────────────────────────────────────────┐
│  🌿 오거서    공지사항  독서리뷰  독토  독서가이드  톡톡  내서재  │  ← GNB (상단 바)
├──────────────────────────────────────────────────┤
│                                                  │
│              페이지 콘텐츠                          │
│              (max-w-5xl mx-auto)                  │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 5.2 적응형 모바일 레이아웃

```
┌──────────────────────┐
│ ☰ 오거서              │  ← 햄버거 + 로고
├──────────────────────┤
│                      │
│   페이지 콘텐츠        │
│                      │
└──────────────────────┘

☰ 클릭 시:
┌──────────┬───────────┐
│ 공지사항   │           │
│ 독서리뷰   │ (오버레이)  │
│ 독토      │           │
│ 독서가이드  │           │
│ 톡톡      │           │
│ 내 서재   │           │
│          │           │
│ ─────── │           │
│ 관리자    │           │
└──────────┴───────────┘
```

### 5.3 관리자 페이지 레이아웃

```
┌───────────┬──────────────────────────────────────┐
│           │  관리자 > 공지사항                       │
│ 대시보드   ├──────────────────────────────────────┤
│ 공지사항   │                                      │
│ 독서리뷰   │        관리 콘텐츠                      │
│ 고전100선  │                                      │
│ 프로그램   │                                      │
│           │                                      │
└───────────┴──────────────────────────────────────┘
```

### 5.4 디자인 토큰 (기존 유지)

| 토큰 | 값 | 용도 |
|------|-----|------|
| primary | hsl(164, 69%, 15%) | 주요 CTA, 헤딩 |
| emerald | hsl(152, 60%, 45%) | 학술/토론 기능 |
| tangerine | hsl(24, 95%, 58%) | 하이라이트, 좋아요 |
| mint | hsl(168, 60%, 58%) | 보조 액션 |
| border-radius | 0.75rem (12px) | 카드 모서리 |
| font-sans | Inter | 본문 |
| font-serif | Playfair Display | 제목 |

---

## 6. API 설계 (목업 백엔드)

### 6.1 공지사항 API

```
GET    /api/notices          ?page=1&limit=10&search=검색어
GET    /api/notices/:id
POST   /api/notices          { title, content, isPinned, attachments }
PUT    /api/notices/:id      { title, content, isPinned, attachments }
DELETE /api/notices/:id
```

### 6.2 독서리뷰 API

```
GET    /api/reviews          ?type=program|ogoseo&category=장학금&page=1&sort=latest
GET    /api/reviews/:id
POST   /api/reviews          { type, programCategory, book, title, content, rating, tags }
PUT    /api/reviews/:id
DELETE /api/reviews/:id
```

### 6.3 고전 100선 API

```
GET    /api/classics          ?year=2024&category=문학/예술&search=검색어
GET    /api/classics/:id
POST   /api/classics          { year, category, title, author, publisher, cover, description }
PUT    /api/classics/:id
DELETE /api/classics/:id
```

### 6.4 프로그램 API

```
GET    /api/programs
POST   /api/programs          { name, description, startDate, endDate }
```

### 6.5 통계 API (관리자)

```
GET    /api/admin/stats       → { noticeCount, reviewCount, classicCount, memberCount }
```

---

## 7. 샘플 데이터 계획

### 7.1 공지사항 (10건)
- 2026 오거서 장학금 모집 안내
- 독서동아리 신규 모집
- 교수-학생 함께 책읽기 프로그램 안내
- 성균 고전 100선 2026년 도서 선정 안내
- 도서관 운영시간 변경 안내
- 독서 챌린지 이벤트
- 오거서 앱 업데이트 안내
- 특별 강연 안내
- 독서 인증서 발급 안내
- 학기 중 프로그램 일정 공지

### 7.2 독서리뷰 (15건)
- 프로그램 후기 10건 (장학금, 동아리, 챌린지 등 분류별)
- 오거서 후기 5건

### 7.3 고전 100선 (30건)
- 문학/예술 10권: 난중일기, 구운몽, 채식주의자, 1984 등
- 인문/사회 10권: 정의란 무엇인가, 목민심서, 국부론 등
- 자연과학 10권: 코스모스, 시간의 역사, 종의 기원 등

---

## 8. 프론트엔드 공통화 전략

### 8.1 공통 컴포넌트

```
components/
├── layout/
│   ├── gnb.tsx               # 상단 GNB (데스크탑)
│   ├── mobile-menu.tsx       # 햄버거 메뉴 (모바일)
│   ├── admin-sidebar.tsx     # 관리자 사이드바
│   └── page-header.tsx       # 페이지 제목 + 브레드크럼
├── shared/
│   ├── data-table.tsx        # 공통 테이블 (정렬, 페이지네이션, 검색)
│   ├── book-card.tsx         # 도서 카드 (썸네일 + 정보)
│   ├── review-card.tsx       # 리뷰 카드 (사용자 + 도서 + 내용)
│   ├── filter-tabs.tsx       # 필터 탭 (pill 스타일)
│   ├── search-bar.tsx        # 검색 바
│   ├── pagination.tsx        # 페이지네이션 (기존 UI 재활용)
│   └── empty-state.tsx       # 빈 상태 표시
├── admin/
│   ├── stat-card.tsx         # 통계 카드
│   ├── admin-table.tsx       # 관리 테이블 (체크박스, 액션)
│   └── form-editor.tsx       # 콘텐츠 편집 폼
└── (기존 컴포넌트 유지)
    ├── bottom-nav.tsx        → 삭제 (GNB로 대체)
    ├── club-detail-modal.tsx → 유지
    ├── kdc-badge.tsx         → 유지
    └── radar-chart.tsx       → 유지
```

### 8.2 공통 훅

```
hooks/
├── use-api.ts              # API 호출 훅 (fetch wrapper)
├── use-pagination.ts       # 페이지네이션 상태 관리
├── use-search.ts           # 검색/필터 상태 관리
└── use-mobile.tsx          # 기존 모바일 감지 (유지)
```

### 8.3 공통 유틸

```
lib/
├── utils.ts                # 기존 cn() 유지
├── api-client.ts           # API 클라이언트 (base URL, 에러 처리)
├── types.ts                # 공통 TypeScript 타입
└── mock-data.ts            # 샘플 데이터 정의
```

---

## 9. 구현 순서

### Phase 1: 인프라 (라우팅 전환 + 레이아웃)
1. App Router 라우팅 구조 생성
2. 반응형 GNB 컴포넌트 구현
3. 모바일 햄버거 메뉴 구현
4. 관리자 사이드바 레이아웃 구현
5. 기존 페이지 컴포넌트 라우트로 이관

### Phase 2: API + 데이터
1. 샘플 데이터 JSON 생성
2. API Route Handlers 구현 (notices, reviews, classics, programs)
3. API 클라이언트 훅 구현

### Phase 3: 새 기능 페이지
1. 공지사항 리스트/상세 페이지
2. 독서리뷰 페이지 (프로그램/오거서 탭)
3. 성균 고전 100선 리스트/상세 페이지
4. 공통 컴포넌트 (DataTable, BookCard, ReviewCard 등)

### Phase 4: 관리자 페이지
1. 대시보드 (통계 카드)
2. 공지사항 CRUD
3. 독서리뷰 관리
4. 고전 100선 관리
5. 프로그램 관리

### Phase 5: 마감
1. 전체 동작 검증
2. 반응형/모바일 테스트
3. 이용자 보고서 작성

---

## 10. 산출물 목록

| # | 산출물 | 형식 | 설명 |
|---|--------|------|------|
| 1 | 프로젝트 기획안 | PROJECT_PLAN.md | 본 문서 |
| 2 | 샘플 데이터 | lib/mock-data.ts | JSON 기반 목업 데이터 |
| 3 | API Routes | app/api/** | Next.js Route Handlers |
| 4 | 이용자 페이지 | app/(user)/** | 6개 메뉴 페이지 |
| 5 | 관리자 페이지 | app/admin/** | 4개 관리 페이지 |
| 6 | 공통 컴포넌트 | components/shared/** | 재사용 가능 UI |
| 7 | 최종 보고서 | FINAL_REPORT.md | 이용자 보고서 |
